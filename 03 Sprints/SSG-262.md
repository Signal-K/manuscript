---
tags:
  - Sprints
  - Sprint-Planning
  - SSG-262
  - SSG-254
  - SSG-263
  - SSG-264
sticker: lucide//copy-x
---
# SSG-262
#SSG-262 
Building off from [[SSG-254]], which I'm temporarily archiving because that sprint resulted in the change of direction and thus, new mechanics that have to be introduced/improved.

We're sticking with the approach of simplifying the project offering initially, to just #DailyMinorPlanet  #PLanetHunters , #CloudspottingOnMars & #JovianVortexHunter. So we need to have every task/mission completed & working, and integrated into the new design system.

We also need to translate the components from the 'new' `app/page.tsx` file over to the new style guide.

We'll also fix the annotation & image resizing issue...by making it full-screen (this is a 2-day sprint. We'll implement the full fix next week).

Tech tree will be shown for each structure, and I'll start designing a better #Archive layout for the structures/viewports & UI blocks that will show parent anomalies/entities.

I'm going to have a mission to share posts, we'll probably just have this update when a user clicks the share button. The painting mechanic will maybe ask users what they think about the creative mechanics? Can ask users to rate proposed projects/features and earn points as well.

I do want there to be more real-world gameplay and uploading of content. I might start downloading Nora Eisner's TESS images too.

If we end up adding the #AreWeAlone project (sending SETI signals to the user), we could say that they have to come from 'completed' planets...so we could have that be another thing that users get periodically and are notified about (as a future feature)

Further TESS sectors?

Add grouped customisations/submissions? This sends a direct link/email back to the researchers?
So users can work on anomalies together.
They can also tie anomalies together - so there's two types of groups. 

Have anomalies floating around in the header (like focumon/plants). Certain actions lead to bonuses? Maybe we animate the structures and put them back in the terrarium?

Maybe we have animations and structures/automatons on the main panel...



Upload your own skies, get new maps for your viewport....

Coral?



We need to get the tabbed architecture, agree on a layout for the blocks.
And the block for the structures and open buttons

Need to create a component for the user to select the planet to scan for clouds...and show the mission to find planet types if they haven't done this yet.
Need to fix the deploy layout for mobile.
fix scrolling on annotation window/component



![[Star Sailors Classifications.csv]]


A simple action game - classifications, instructions for rovers, content/actions from other characters

![[Pasted image 20250719223149.png]]

JIRA:SSG-262 

# SSG-263
#SSG-263
Quick patch:
* [x] Fix profile menu (remove name) ✅ 2025-07-20
	* A note that this is a temporary change/patch - not a "fix"
* [x] Remove/hide landmarks text (until we actually add something there) in #ActivityHeader component ✅ 2025-07-20
* [x] Hide data history in navbar for now (again, until there's a purpose to it) ✅ 2025-07-20
* [x] Hide entries in `linked_anomalies` table if they've been classified already (by that user) ✅ 2025-07-20
* [x] Add milestones for this week (show them in header?) ✅ 2025-07-20
* [x] Add custom domain to supabase ✅ 2025-07-20
![[Pasted image 20250720160700.png]]


# SSG-264
Would be nice to have some pointers to open structures and view the archive buttons
Add DMP

* [ ] Fix GCP domain tld
* [ ] Add levelling system to structures alongside tech tree - the more they're used... ^286e3e
* [ ] New home page?
* [] More sectors
* [ ] Add fetch recent tic and upload to workflow in gh
* [x] Add more colour/variance to viewports ✅ 2025-07-25
* [ ] Create viewport (customisation option later) for cloudspotting
* [x] Add DailyMinorPlanet to `linked_anomalies` creation and to viewport ✅ 2025-07-21
		Maybe this is an unlockable mission?
		Maybe milestones could be set per-user (another service for cron-jobs) based on unlocked missions.
		Add a vote/feedback area, because I'm slightly uncomfortable (in the longer-term) about hiding projects behind a progress barrier.
		*  [ ] Add annotation method for #DailyMinorPlanet 
	![[Pasted image 20250721220058.png || 320]]
 ^884474
* [x] Add comment/surveyor feed to surveyor page ✅ 2025-07-21
	* [ ] Building on from this, we may need to combine the commenting & surveying pages, or change the design so they don't look (structurally, anyway) the same
	* [ ] Proper action/scene transition for post-comment mission
	* [x] Put classification scene into structure (viewport serves as background) ✅ 2025-07-26
	*![[Pasted image 20250721145301.png || 200]]
* [ ] Users can select the sector when they deploy their telescope?

* [x] BUG: `linked_anomalies` being created that point towards classifications user has already made. Maybe we point to more sectors, same anomaly ✅ 2025-07-26
* [x] BUG: Telescope viewport sidebar has an incorrect overlap ✅ 2025-07-24
* [x] BUG: Clouds that have been classified aren't removed from `linked_anomalies` table JIRA:SSC-123 #SSC-133 JIRA:SSC-133 ✅ 2025-07-24
* [x] BUG: Telescope viewport in `/structures/telescope` route is not scrollable ✅ 2025-07-24
* [x] BUG: Votes table has differences between prod & local dev environments ✅ 2025-07-21
	* ![[Pasted image 20250721145848.png || 200]]

* [ ] BONUS: Users can control automatons in the viewport and see collisions/events (will need to update viewport to have multiple views/screens)
	* Can users upload their own images (backgrounds) to "collect" for the viewport?
* [ ] Add the deploy method to the main telescope viewport
	* [ ] We'll add a list of minigames/actions to the main viewport at some point, right now (21/07/25) I'm working on splitting up the telescope viewport so we can easily add more sections.
	* [x] At minimum, if user hasn't deployed their telescope, we redirect them in this view ✅ 2025-07-26
	* [x] Fix telescope right-hand-side colours ✅ 2025-07-26
	* [x] Some sort of jumping or interaction action for launching things that are ready to do someth? ✅ 2025-07-26

#### Skill tree
1. We'll say that once a user has "discovered" 4 planets (let's not worry about consensus/voting for now, I still need to figure out how to explain the concepts to end-users), they can unlock the ability to find asteroids in the viewport. [[#^286e3e]] [[#^884474]]
This allows us to go back to the level system in the old #Baseplate component/layout directory

Show skill mastery in each box

https://www.reddit.com/r/godot/s/Uxv56Z7fm2
![[Pasted image 20250721153958.png || 300]]




Add quick deploy buttons to the #ActivityHeader here: ![[Pasted image 20250721154028.png]]

We need to add this `deploy` code to the #Telescope #Viewport :
```tsx
  const handleDeploy = async () => {
    if (!session?.user?.id || !selectedSector || alreadyDeployed) return
    setDeploying(true)
    const seed = selectedSector.x * 1000 + selectedSector.y
    const selectedAnomalies = tessAnomalies
      .filter((_, i) => Math.floor(seededRandom1(seed, i) * 10) < 3)
      .slice(0, 4)
    if (selectedAnomalies.length === 0) {
      setDeploymentMessage("No TESS anomalies found in selected sector.")
      setDeploying(false)
      return
    }
    const inserts = selectedAnomalies.map(anomaly =>
      supabase.from("linked_anomalies").insert({
        author: session.user.id,
        anomaly_id: anomaly.id,
        classification_id: null,
        automaton: "Telescope",
      })
    )
    const results = await Promise.all(inserts)
    const hasError = results.some(r => r.error)
    setDeploymentMessage(
      hasError
        ? "Error deploying telescope. Please try again."
        : `Telescope successfully deployed! ${selectedAnomalies.length} TESS exoplanet candidates are now being monitored.`
    )
    setAlreadyDeployed(!hasError)
    setDeploying(false)
  }
```
Then, we can update the viewport to show available missions and filter classifications/anomalies by mission state/ready status

### Cron jobs
### Go milestones
Let's just figure out what users have completed and then present them milestones. Probably a table like `linked_anomalies` for this would work fine if we clear it on a regular basis.

![[Pasted image 20250721231918.png]]


Notification system is up next!

> Reproduce the skill tree everywhere, for everything!
## References
We can do someth with active asteroids, maybe? Minimum, new skill
Add more content to navigation/targets
Open chats/idea generations with chatgpt: 
1. https://chatgpt.com/share/687e07eb-64ec-800f-87e0-4c38d005000f

![[IMG_20250722_104316258.jpg]]

![[IMG_20250722_104318680.jpg]]


Get GPT to design the obsidian vault/quartz


The key pieces of feedback from the last release is that we need things to be clearly described and ensure that everything works (e.g. no missing files...)

Next week - sunspots